using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProcedualPillar : MonoBehaviour {

	// Use this for initialization
	void Start () {
		PillarData p = new PillarData ();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
